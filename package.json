{
  "name": "web-fig",
  "version": "1.0.0",
  "description": "WhatsApp Sticker Bot (Meta Cloud API)",
  "scripts": {
    "install": "npm --prefix backend install",
    "start": "npm --prefix backend start"
  }
}
